## memo

`git push -u origin main` -> `iron` respository

`git push -u master main` -> `github.io` respository

```bash
$ git branch -a
* main
  remotes/master/main
  remotes/origin/main
```

## TODO

更新`e-linkedin`字段
